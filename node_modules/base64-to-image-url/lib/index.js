"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.base64ToImageUrl = void 0;
var base64ToImageUrl = function (base64String) {
    var blob = base64ToBlob(base64String);
    var url = URL.createObjectURL(blob);
    return url;
};
exports.base64ToImageUrl = base64ToImageUrl;
var base64ToBlob = function (base64String) {
    var byteCharacters = atob(base64String);
    // Use Uint8Array.from to create a Uint8Array directly
    var byteArrays = new Uint8Array(Array.from(byteCharacters, function (char) { return char.charCodeAt(0); }));
    var blob = new Blob([byteArrays], {
        type: "image/png",
    });
    return blob;
};
